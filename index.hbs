{{!< default}}

<div id="latest-post" class="container">
    {{#foreach posts}}
        {{#if @first}}
            {{> post-content}}
        {{/if}}
    {{/foreach}}
</div>

<div id="post-index" class="container">
    <ol class="post-list">
        {{#foreach posts}}
            <li class="post-stub {{post_class}}" >
                <a class="js-ajax-link"  title="{{title}} | {{@blog.title}}" href="{{url}}">
                    <h4 class="post-stub-title">{{title}}</h4>

                    <time class="post-stub-date" datetime="{{published_at}}">发布于 {{date format="MM dd YYYY"}}</time>

                    {{#if featured}}
                        <span class="post-stub-tag">精选</span>
                    {{/if}}
                </a>
            </li>
        {{/foreach}}
    </ol>

    <div class="post-navigation">
        {{pagination}}
    </div>
</div>